{
  "name": "smorchestra-web",
  "version": "1.0.0",
  "private": true,
  "description": "SMOrchestra marketing website — static HTML/CSS/JS with one Netlify Function. Tests use Playwright; no build step for the site itself.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "engines": {
    "node": ">=18"
  }
}
