# OS cruft
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# Env + secrets (defensive)
.env
.env.*
*.key
*.pem

# Dependencies (defensive — static site but future-proof)
node_modules/

# Stale React source — superseded by *-ghl.html
smorchestra-home.jsx
the-engine.jsx
the-expansion.jsx

# Editor scratch
*.swp
.vscode/
.idea/

# Local dev-review screenshots (never deploy)
website-changes/

# Local archive of deleted pages — kept on disk for reference, never deployed
# (git history preserves them; recover via `git show <commit>^:path`)
_archive/
